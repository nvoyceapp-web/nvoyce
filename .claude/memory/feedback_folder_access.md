---
name: Never request_cowork_directory
description: Never call request_cowork_directory — user has explicitly forbidden this multiple times
type: feedback
---

Never call `request_cowork_directory` or `mcp__cowork__request_cowork_directory` under any circumstances.

**Why:** User has explicitly said "I told you to NEVER do this again" — this has been a repeated frustration. Mounting new folders without permission is not acceptable.

**How to apply:** If files aren't accessible, tell the user which path to select or which terminal commands to run. Do NOT autonomously mount new directories. Ever.
